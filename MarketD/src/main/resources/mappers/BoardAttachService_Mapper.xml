<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.team.marketd.persistence.BoardAttachService_Mapper">


	<insert id="insert" parameterType="bav"> <!-- -->
	
		insert into boardattach32(uuid, uploadpath, filename, filetype, pidx)
		values(#{uuid}, #{uploadPath}, #{fileName}, #{fileType}, #{pidx})
	
	</insert>

	
	<delete id="delete">
	
		delete from tbl_attach where uuid=#{uuid}	
	
	</delete>
	
	<select id="findByBno"  resultType="Integer"><!--   -->
	
		select * from product32 A inner join boardattach32 B on A.pidx=B.pidx where A.pidx=#{pidx}  
			 
	</select>
	
	<select id="checkproduct" parameterType="HashMap" resultType="Integer">
	 select pidx from product32 where midx =#{midx} and caidx =#{caidx}
	 and psubject =#{psubject} and pcontent =#{pcontent} and pvol =#{pvol} 
	 and pmoney =#{pmoney} and pfee =#{pfee} and pip =#{pip}
	</select>
	
</mapper>